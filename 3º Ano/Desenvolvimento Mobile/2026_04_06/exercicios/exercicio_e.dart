// E) Escreva um programa em Dart que peça ao usuário os tamanhos dos catetos de um triângulo retângulo e mostre na tela o valor de sua hipotenusa.
import 'dart:math';

void main() {
    double a, b = 3.0, 4.0;
    double c = sqrt(pow(a, 2) + pow(b, 2));
    print("A = $a; B = $b; C = $c");
}